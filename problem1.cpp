#include<stdio.h>
int main(){
	int a ,b, total;
	printf("Enter the number assignment to be typed: ");
	scanf("%d", &a);
	printf("Enter the number of assignment to be printed: ");
	scanf("%d, &b);
	total = a*3 + b*5;
	printf("%d",total);
	_
	}
